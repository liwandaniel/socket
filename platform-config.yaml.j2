apiVersion: v1
kind: ConfigMap
metadata:
  name: platform-config
data:
  # 配置单租户模式还是多租户模式，默认 enabled 为多租户，disabled 单租户
  tenantMode: "{{ tenantMode }}"
  compassWebEndpoint: "http://{{ systemEndpoint }}:6060"
  cleverServer: "http://{{ systemEndpoint }}:6061"
  oidcIssuer: "http://{{ systemEndpoint }}:6062"
  userwebServer: "http://{{ systemEndpoint }}:6063"
  hodorEndpoint: "http://{{ systemEndpoint }}:6002"
  devopsWebhookURL: "http://{{ systemEndpoint }}:6002/apis/admin.devops.caicloud.io/v1/pipelines"
  userwebBasePath: ""
  consoleBasePath: ""
  ssoBasePath: ""
