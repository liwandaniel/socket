_config:
  _metadata:
    description: logging-admin
    name: logging-admin
    namespace: default
    template:
      type: template.caicloud.io/application
      version: 1.0.0
    version: "v2.3.0-beta.0"
  controllers:
  - containers:
    - image: '[[ registry_release ]]/logging-admin:v2.3.0-beta.0'
      imagePullPolicy: IfNotPresent
      ports:
      - port: 7800
        protocol: TCP
    controller:
      replica: 1
      strategy:
        surge: 1
        unavailable: 0
    services:
    - name: logging-admin
      ports:
      - port: 7800
        protocol: TCP
      type: ClusterIP
    type: Deployment
