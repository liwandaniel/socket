_config:
  _metadata:
    description: Tutorial Gitbook
    name: tutorial
    namespace: default
    template:
      type: template.caicloud.io/application
      version: 1.0.0
    version: "v1.0.0"
  controllers:
  - containers:
    - env:
      - name: PRODUCTION
        value: 'compass'
      image: '[[ registry_release ]]/tutorial:v1.0.0'
      imagePullPolicy: IfNotPresent
      ports:
      - port: 4000
        protocol: TCP
    controller:
      replica: 1
      strategy:
        surge: 1
        unavailable: 0
    services:
    - name: tutorial
      ports:
      - protocol: TCP
        port: 4000
      type: ClusterIP
    type: Deployment
