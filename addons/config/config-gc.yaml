_config:
  _metadata:
    description: config-gc
    name: config-gc
    namespace: default
    template:
      type: template.caicloud.io/application
      version: 1.0.0
    version: "v0.2.7"
  controllers:
  - containers:
    - env:
      - name: CONFIG_ADMIN_URL
        value: http://config-admin:8080
      image: '[[ registry_release ]]/config-gc:v0.2.7'
      imagePullPolicy: IfNotPresent
      resources:
        limits:
          cpu: 500m
          memory: 500Mi
        requests:
          cpu: 300m
          memory: 200Mi
    controller:
      replica: 1
    type: Deployment
