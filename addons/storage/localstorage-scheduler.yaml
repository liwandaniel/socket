_config:
  _metadata:
    description: localstorage-scheduler
    name: localstorage-scheduler
    namespace: kube-system
    template:
      type: template.caicloud.io/application
      version: 1.0.0
    version: "v0.5.0-alpha.1"
  controllers:
  - containers:
    - env:
      - name: ENV_LOG_LEVEL
        value: info
      - name: ENV_LISTEN_PORT
        value: '32233'
      image: '[[ registry_release ]]/storage-localstorage-scheduler:v0.5.0-alpha.1'
      imagePullPolicy: IfNotPresent
      ports:
      - port: 32233
        protocol: TCP
    controller:
      replica: 1
      strategy:
        surge: 1
        unavailable: 0
    services:
    - name: localstorage-scheduler
      ports:
      - port: 32233
        protocol: TCP
      type: ClusterIP
    type: Deployment
