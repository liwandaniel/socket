apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kubeflow-admin-postgres-data
  namespace: [[ default_namespace ]]
  labels:
    caicloud-app: kubeflow-admin-postgres-data
spec:
  storageClassName: heketi-storageclass
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4096Mi
