apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: notify-admin-postgres-data
  namespace: [[ default_namespace ]]
  labels:
    caicloud-app: notify-admin-postgres-data
spec:
  storageClassName: heketi-storageclass
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4096Mi
