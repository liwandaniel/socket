apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cds-mongo-data
  namespace: [[ default_namespace ]]
  labels:
    caicloud-app: cds-mongo-data
spec:
  storageClassName: heketi-storageclass
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4096Mi
