apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: helm-registry-charts-data
  namespace: [[ default_namespace ]]
  labels:
    caicloud-app: helm-registry-charts-data
spec:
  storageClassName: heketi-storageclass
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4096Mi
