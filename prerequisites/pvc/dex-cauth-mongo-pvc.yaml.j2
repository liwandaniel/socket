apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dex-cauth-mongo-data
  namespace: [[ default_namespace ]]
  labels:
    caicloud-app: dex-cauth-mongo-data
spec:
  storageClassName: heketi-storageclass
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4096Mi
